
# PyPlaceRoulette Settings file

settings = {
   
    # Replace COOKIE with your ROBLOSECURITY key in order to successfully make web requests or else Roblox will think that you're a non-user.
    # Do NOT share the key with anyone.

    "SecurityKey": "COOKIE",
    
    # Skip starter (new user) places
    "SkipStarterPlaces": True,

    # View place webpage in your browser
    "ViewPlaceInWebBrowser": True,
    
    # work in progress
    "SkipPrivatePlaces": True,

}
